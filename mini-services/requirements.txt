# VaakSetu Local AI Service — Python Dependencies
# Install with: pip install -r requirements.txt
#
# Recommended setup:
#   python -m venv venv
#   source venv/bin/activate  (macOS/Linux)
#   .\venv\Scripts\Activate.ps1  (Windows PowerShell)
#   pip install -r mini-services/requirements.txt

# ── Core API Framework ─────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# ── PyTorch (install platform-specific wheel separately if needed)
# For CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu121
# For CPU:  pip install torch
torch>=2.1.0

# ── AI4Bharat IndicTrans2 ─────────────────────────────────────
# IndicTrans2 remote code requires transformers 4.38.x - 4.41.x (not 5.x)
transformers>=4.38.0,<4.42.0
tokenizers>=0.19.0,<0.20.0
sentencepiece>=0.1.99
sacremoses>=0.1.1

# IndicTransToolkit (official pre/post processor for IndicTrans2).
# It builds a native extension. Windows uses the NLLB fallback unless the
# optional package is installed separately after enabling the MSVC workload.
IndicTransToolkit>=0.1.0; sys_platform != "win32"

# ── Whisper ASR ────────────────────────────────────────────────
# faster-whisper uses CTranslate2 for fast CPU/CUDA inference
faster-whisper>=1.0.0

# ── TTS Engines ────────────────────────────────────────────────
edge-tts>=6.1.9
gTTS>=2.5.0

# ── Audio Processing ───────────────────────────────────────────
pydub>=0.25.1
# ffmpeg-python is optional (we call ffmpeg via subprocess directly)

# ── NLLB Fallback ─────────────────────────────────────────────
# facebook/nllb-200-distilled-600M downloads automatically via HF Hub
accelerate>=0.27.0

# ── Hugging Face Hub ──────────────────────────────────────────
huggingface-hub>=0.22.0

# ── File / Async Utilities ────────────────────────────────────
aiofiles>=23.0.0
python-multipart>=0.0.9
